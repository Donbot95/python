#Ask the user for a number. Depending on whether the number is even or odd, print out an appropriate message
#If the number is a multiple of 4, print out a different message.


number = input("Enter a number: ")

if (int(number) %4) == 0:
    print("{} is a multiple of 4.".format(number))
elif (int(number) %2) == 0: 
    print("{} is even.".format(number))
elif (int(number) %2) !=0:
    print("{} is odd.".format(number))
    

#Ask the user for two numbers: one number to check (call it num) and one number to divide by (check). 
#If check divides evenly into num, tell that to the user. If not, print a different appropriate message.

num = input("Enter a number to check division: ")
check = input("Enter a number to divide by: ")

if (int(num) %(int(check))) == 0:
    print("{} divides evenly by ".format(num) + "{}".format(check))
elif (int(num) %(int(check))) != 0:
    print("{} is not divisible by ".format(num) + "{}".format(check))
